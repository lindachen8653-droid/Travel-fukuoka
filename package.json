{
  "name": "our-trip-fukuoka",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply our-trip-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply our-trip-db --remote"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "wrangler": "^4.33.1"
  }
}
